我将分三步完成前后端对接，并确保所有演示账号可用：

### Phase 1: 后端数据准备 (Backend)
为了保留演示账号，我们需要在后端数据库中预置这些用户数据。
1.  **扩展用户模型**：修改 `User` 模型，增加 `profile` 字段（JSON格式），用于存储前端展示所需的头像、学校、年级、简介等详细信息。
2.  **创建初始化脚本**：编写 `backend/app/initial_data.py`，将前端 `MOCK_USERS` 中的所有账号（学生、讲师、管理员等）及初始组织数据写入数据库。密码统一预设为 `123456`。

### Phase 2: 前端 API 接入 (Frontend)
1.  **创建 API 客户端**：新建 `frontend/lib/api-client.ts`，封装 `fetch` 请求，处理 Token 存储、自动附加 Header 以及错误拦截。
2.  **配置代理**：修改 `next.config.js`，配置 API 代理转发，解决跨域问题。

### Phase 3: 改造核心逻辑
1.  **重构用户上下文 (`user-context.tsx`)**：
    *   将 `login` 方法改为调用后端 `/api/v1/auth/login/access-token` 接口。
    *   登录成功后，并行调用 `/api/v1/auth/me` (基本信息) 和 `/api/v1/match/points/balance` (积分) 来组装完整的用户对象。
2.  **保留演示体验**：前端登录页的“快速登录”按钮逻辑保持不变，因为后端已经预置了对应的账号密码，点击即可真实登录。

**验证计划：**
*   执行初始化脚本，填充数据库。
*   启动前后端服务。
*   在前端点击“快速登录（学生账号）”，验证能否成功登录并显示正确的头像和积分。
